package newproject;

import java.util.*;

public class test {
	public static void main(String[] args) {
		HashMap<String,String> new1 =new MysqlConnection().wordList("example");
		
		Set<String> keys = new1.keySet();
		Iterator<String> it = keys.iterator();
		while(it.hasNext()) {
			String word = it.next();
			String meaning = new1.get(word);
			System.out.println(word + " : " + meaning);
			}
		
		
//		String[] a = new String[2];
//		String lang = "Kor";
//		String search_word = "예시";
//		a = new BasicVocaSearch(lang,search_word).search();
//		System.out.println(a[0]+","+a[1]);
	}

}
